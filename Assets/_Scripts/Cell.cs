using UnityEngine;

public enum State
{
    Hover,
    Clicked,
    Revealed
}

public class Cell : MonoBehaviour
{
    private void HandleStates()
    {
        switch (State)
        {
            case (State.Hover)
            {
                
            }
        }
    }
}
